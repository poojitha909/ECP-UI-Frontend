import { ActiveMaxDirective } from './active-max.directive';

describe('ActiveMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveMaxDirective();
    expect(directive).toBeTruthy();
  });
});
