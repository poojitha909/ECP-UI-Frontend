import { TrapModalFocusDirective } from './trap-modal-focus.directive';

describe('TrapModalFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TrapModalFocusDirective();
    expect(directive).toBeTruthy();
  });
});
